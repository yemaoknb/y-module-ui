/*
 * @Description: prettier规则配置
 */
module.exports = {
  semi: false, // 声明结尾使用分号(默认true)
  singleQuote: true, // 使用单引号（默认false）
  printWidth: 80, // 每行代码长度（默认80）
  trailingComma: 'none', // 对象没有尾随逗号
  arrowParens: 'avoid',
  endOfLine: 'auto' // 去除换行符校验
}
